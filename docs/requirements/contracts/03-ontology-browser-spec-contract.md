GOAL: Spec must define the master-detail tree browser with global selection, hypertext navigation, source indicators, conflict indicators, and color-coded relationships.
CONSTRAINTS: Must use existing React/TanStack/Shadcn stack. Must work with data from existing API endpoints. Must improve on Protege's patterns (not copy them).
FAILURE CONDITIONS: SHALL NOT include source switching logic (that's 04). SHALL NOT require new backend endpoints beyond what 01 provides.
