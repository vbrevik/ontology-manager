GOAL: Spec must define both the sidebar quick-switch component and the /ontology-sources management page with cards, stats, and import/swap/layer actions.
CONSTRAINTS: Must integrate with existing workspace switcher pattern. Must use existing Shadcn components. Must call APIs from 01 and 02.
FAILURE CONDITIONS: SHALL NOT redefine the browser (that's 03). SHALL NOT add backend endpoints (those are in 01/02).
