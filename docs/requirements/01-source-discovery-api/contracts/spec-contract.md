GOAL: Synthesize spec.md + research + interview into a complete claude-spec.md that captures all requirements for the source discovery API.
CONSTRAINTS: Must incorporate unique constraint change, auth requirement, per-source versioning. Must not add implementation decisions beyond what spec + interview decided.
FAILURE CONDITIONS: SHALL NOT omit requirements from any input source. SHALL NOT include architecture or implementation choices.
